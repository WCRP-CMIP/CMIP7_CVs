name: Check JSON-LD Graph

on:
  push:
    branches:
      - production # Adjust if your GitHub Pages branch differs
  repository_dispatch: # Triggered by another workflow


jobs:
  update-jsonld:
    runs-on: ubuntu-latest  # Specify the machine type
      
    steps:
    
    # - name: Checkout Code
    #   uses: actions/checkout@v4
    #   with:
    #     fetch-depth: 0

    - name: Ensure the JSON-LD files are functional
      id: check-graph
      uses: WCRP-CMIP/CMIP-LD/actions/check-graph@main


